namespace JobTracker.Domain.Entities;

// Thin domain marker - the actual Identity-derived class lives in Infrastructure.
// JobApplication references the user by string UserId (ASP.NET Identity PK).
